= form_for @<%= singular_table_name %> do |form|
  - if @<%= singular_table_name %>.errors.any?
    #error_explanation
      %h2= t('form_error_title')

<% for attribute in attributes -%>
  = form.input :<%= attribute.name %>
<% end -%>
  = f.submit 'Save'
